# Restart Backend Server

## Quick Steps:

1. In the terminal where backend is running (Terminal 3), press **Ctrl+C**

2. Run:
```bash
node server.js
```

That's it! The server will now have the notes routes available.

## What to expect:
- Server will show database column updates
- "Server running on http://localhost:3000"
- No errors about missing routes

